#Parâmetros  
source('parametros1.R',local=TRUE, encoding = "UTF-8")

#Equações
source('equacoes1.R',local=TRUE,encoding = "UTF-8")

#Cálculo 
source('calculo1.R',local=TRUE, encoding = "UTF-8")

#Gráfico
source('grafico1.R',local=TRUE, encoding = "UTF-8")

#Dados
source('dados1.R',local=TRUE, encoding = "UTF-8")

#Respostas
source('resposta1.R',local=TRUE, encoding = "UTF-8")